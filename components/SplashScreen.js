import React from 'react';
import {ActivityIndicator, Text, View} from 'react-native';

const SplashScreen = () => {
  return (
    <View
      style={{flex: 1, justifyContent: 'center', backgroundColor: '#222f3e'}}>
      <Text>Aqui va un icono</Text>
    </View>
  );
};

export default SplashScreen;